struct Monkey {
    items: Vec<u32>,
    operator: String,
    operand: u32,
    test: u32,
    falsy: u32,
    truthy: u32,
    operations: u32,
}

fn part_one() -> u32 {
    10
}

fn main() {
    println!("{}", part_one());
}
